# SwapIt Marketplace (Flutter)

This is a scaffolded Flutter project matching the UI and structure you provided.

- Cart page, Sell item page, and Profile page implemented to match the design.
- Other screens are placeholders styled to match the app theme.
- Uses flutter_bloc for Cart state management.

To run:
1. Install Flutter SDK.
2. Run `flutter pub get`
3. Open in your editor and run on a device/emulator.

Notes:
- Assets are placeholders in `assets/images/`.
- Replace models/backends with real implementations as needed.
